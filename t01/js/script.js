'use strict';

let greetings = 'Hello JavaScript from outside!';
alert(greetings);
//В жабаскрипт можно объявлять переменные с помощью let и с помощью var,
//но лучше let, так как var более глобальна и в целом это общепринятое правило.